# 实验记录：Stage 01 Day 02 Package and CLI Baseline

## 1. 目标

验证 ForgeLLM 能否在不修改既有 `SCI` 环境的前提下，建立独立 `.venv`、完成 editable install，并通过最小 CLI、lint、严格类型检查、单元测试和 CPU Smoke。

## 2. 环境

- 平台：Windows 11 / win32
- GPU：NVIDIA GeForce RTX 4070 Laptop GPU 8188 MiB（本次未使用）
- Python：3.12.3
- PyTorch：未安装，本阶段不需要
- CUDA：系统 Toolkit 12.4，本次未使用
- Conda 环境：不使用；采用仓库内 `.venv`
- 代码路径：`C:\Users\27475\Desktop\Resume_Project\ForgeLLM`
- 代码 commit / diff：基线 commit `f934b00538238991c136c5726845ca3b34bb150d`，工作区包含 Month 01 计划与 Day 1/2 变更
- 数据集路径：无

## 3. 实验变量

- 主变量：加入最小可安装包、CLI 与开发质量工具链
- 固定项：Windows 主机、Python 3.12.3、CPU-only、无业务数据、无训练依赖
- 对比对象：只有占位 `pyproject.toml`、无包、无 CLI、无测试的初始化骨架

## 4. 命令

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\python.exe -m ruff check src tests
.\.venv\Scripts\python.exe -m mypy
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\forgellm.exe --help
.\.venv\Scripts\forgellm.exe doctor
```

## 5. 输出路径

- 独立环境：`.venv/`（Git 忽略）
- 包代码：`src/forgellm/`
- 测试：`tests/unit/test_cli.py`、`tests/smoke/test_module_entrypoint.py`
- 依赖基线：`pyproject.toml`、`requirements-dev.lock`
- 权重/模型/数据：无

## 6. 结果

| 指标 | 数值 |
|---|---:|
| editable install | 通过 |
| Ruff | 通过，0 个问题 |
| mypy strict | 通过，6 个源文件无问题 |
| pytest | 4/4 通过 |
| CPU Smoke | 通过 |
| CLI `--help` | 返回 0 |
| CLI `doctor` | 返回 0，输出项目/Python/平台版本 |

已验证的直接开发依赖：

- pytest 8.4.2；
- mypy 1.20.2；
- ruff 0.15.21。

## 7. 失败与异常

- 现象：沙箱内首次 editable install 无法连接包索引；
- 报错：`WinError 10013`，无法下载 `setuptools>=69`；
- 判断：网络沙箱限制，不是项目元数据错误；
- 处理：获得受控网络授权后重试，安装成功。实际使用已配置的清华 PyPI 镜像。

## 8. 结论

Day 2 假设在当前 Windows/Python 3.12 环境下成立：项目可隔离安装，最小 CLI 和质量工具链均能执行。该结果只证明本地 Windows 基线，不证明 Linux CI 或陌生环境复现；因此 G0 尚未通过。

## 9. 下一步

进入 Day 3：设计配置校验、结构化日志、Run ID 和非敏感运行元数据；随后在 Day 4/5 补齐统一命令、CI 与陌生环境复现。

