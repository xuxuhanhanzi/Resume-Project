# ForgeLLM Stage 4 supervised post-training dependencies.
# Versions were frozen from official PyPI stable releases on 2026-07-28.
# Install after requirements-model.lock so the existing CUDA-enabled Torch is reused:
#   .\.venv\Scripts\python.exe -m pip install -r requirements-post-training.lock

accelerate==1.14.0
bitsandbytes==0.49.2
datasets==5.0.0
peft==0.19.1
transformers==5.14.1
trl==1.8.0

# ForgeMM local vision-language inference (paired with torch==2.6.0+cu124).
Pillow==11.1.0
torchvision==0.21.0+cu124; platform_system == "Windows"
